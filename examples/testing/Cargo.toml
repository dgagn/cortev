[package]
name = "testing"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.9"
cortev = { path = "../../cortev", features = ["session-memory"] }
tokio = { version = "1.41.1", features = ["full"] }

[lints]
workspace = true
